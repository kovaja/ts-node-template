{
  "name": "kovaja-ts-node-template",
  "version": "1.0.1",
  "description": "Empty project with template for node.js application with REST api and react frontend",
  "scripts": {
    "validate": "node ./tools/validation.js",
    "server": "cd server && npm run prod",
    "start": "npm run server",
    "publish": "git push origin master && git push heroku +master && heroku logs --tail -a ts-node-template",
    "publish:patch": "npm run validate && npm version patch && npm run publish",
    "publish:minor": "npm run validate && npm version minor && npm run publish",
    "publish:major": "npm run validate && npm version major && npm run publish",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev"
  },
  "keywords": [
    "node",
    "typescript"
  ],
  "author": "kovaja",
  "license": "ISC"
}
